Trello
Run npx prisma studio open my prisma studio
Alt + arrow up / down to move line of code

git log origin/main --oneline //view all push history
git log --oneline   //View all commit history , not pushed


PSQL
\l --view DBs
\c db_name --connect to table
\dt --view table /list tables
\dt table_name or \dt "table_name" --view table
SELECT * FROM "table_name"; -view ROWS

//View expeses by user or userId
SELECT * FROM "Expense"
ORDER BY "userId", "date" DESC;

SELECT * FROM "Expense"
WHERE "userId" = 1
ORDER BY "date" DESC;
